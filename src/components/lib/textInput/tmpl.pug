mixin textInput({ mix, isFullWidth = true, isCompact = false } = {}, attrs = {})
  -
    const { b, c } = locals;
    const textInput = b('textInput');
  input(
    class=c(textInput({ fullWidth: isFullWidth, compact: isCompact }).mix(mix && mix.block)),
    type='text'
  )&attributes(attrs)
